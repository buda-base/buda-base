[Unit]
Description=Couchdb service
After=network.target
After=syslog.target

[Service]
Type=simple
User=couchdb
Group=couchdb
ExecStart=<%= ENV['DATA_DIR'] %>/couchdb/bin/couchdb -o /dev/stdout -e /dev/stderr
Restart=always

[Install]
WantedBy=multi-user.target
