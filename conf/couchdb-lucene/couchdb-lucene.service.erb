[Unit]
Description=Couchdb service
After=network.target
After=syslog.target

[Service]
Type=simple
User=couchdb
Group=couchdb
ExecStart=<%= ENV['DATA_DIR'] %>/couchdb-lucene/bin/run
Restart=always

[Install]
WantedBy=multi-user.target
